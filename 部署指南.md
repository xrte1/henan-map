# 豫 · 风土方舆 — 河南县域 2.5D 文旅地图（公开网站版）

把本文件夹整体上传到任意静态网站托管服务即可上线，**无需服务器、无需数据库**：
`index.html` 是一个完全自包含的单文件网页（地图边界、157 县资料、73 景区、50 处古都/名人/帝王标点全部内嵌，无任何外部依赖）。

## 一、最快：GitHub Pages（推荐，免费、永久、可绑自己的网址）

1. 打开 https://github.com/new 新建仓库（Public），如 `henan-map`。
2. 把本文件夹内容推上去（或网页端直接拖文件上传）：
   ```bash
   git init
   git add .
   git commit -m "init"
   git remote add origin https://github.com/<你的用户名>/henan-map.git
   git push -u origin main
   ```
3. 仓库 Settings → Pages → Source 选 `Deploy from a branch` → `main` + 根目录 `/` → Save。
4. 一两分钟后访问：`https://<你的用户名>.github.io/henan-map/`
   （把链接发给别人就能看了。）

## 二、Netlify（拖拽即上线）

1. 打开 https://app.netlify.com/drop （登录后）。
2. 把本文件夹**整个拖进**页面即可，马上得到一个 `https://xxxx.netlify.app` 的网址。

## 三、Vercel

1. 打开 https://vercel.com/new ，导入包含本文件夹的 Git 仓库，或使用 CLI：
   ```bash
   npm i -g vercel && vercel
   ```
2. 部署后获得 `https://xxxx.vercel.app`。

## 四、Cloudflare Pages

1. https://dash.cloudflare.com → Workers & Pages → Create → Pages → 上传本文件夹。
2. 获得 `https://xxxx.pages.dev`。

## 五、自有服务器 / 内网

- 任意 Web 服务器把 `index.html` 放根目录即可（Nginx / Apache / IIS / OSS+CDN）。
- 电脑临时共享给局域网同事：
  ```bash
  cd 本文件夹 && npx serve .
  ```
  或
  ```bash
  python -m http.server 8080
  ```
  同网段访问 `http://你的IP:8080`。

## 说明

- 人口为 2020 年七普口径约数；文化底蕴/特产/景区/古都名人为 AI 整理的科普性导览，正式使用请以官方资料为准。
- 想改标题、配色、加页脚版权/备案信息、或换自定义域名：直接编辑 `index.html` 里对应的文字即可。
- 可选：首页 SEO 与分享卡片（`<title>`、`<meta name="description">`）已内置，如需社交分享大图可再加 `og:` 标签。
